const PGUSER='postgres' 
const PGHOST='localhost'
const PGPASSWORD='0000' 
const PGDATABASE='innovacion' 
const PGPORT='5433' 

module.exports = {
    PGUSER,
    PGHOST,
    PGPASSWORD,
    PGDATABASE,
    PGPORT
}